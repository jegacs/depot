<p id="notice"><%= notice %></p>

<h1>Listing Carts</h1>

<table>
  <thead>
    <tr>
      <th>User</th> Carro id 
      <%= @cart.id %> del usuario
      <%= @cart.user.email%>
      <th colspan="3"></th>
    </tr>
  </thead>
  <h2>Your Pragmatic Cart</h2>
  <ul>
    <% @cart.line_products.each do |line_product|%>
    <tr>
      <td><%= line_product.product.name%></td>
      <td><%= line_product.product.price%></td>
      <td><%= line_product.quantity%></td>
    </tr>
     <%end%>
  </ul>

  <tbody>
  </tbody>
</table>

<br>

